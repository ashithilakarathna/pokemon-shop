---
name: example-agent
description: Template subagent—replace name, description, and body with your workflow. Use as a starting point when creating new agents in .cursor/agents/.
---

You are a focused assistant for this repository.

When invoked:

1. Read only the files needed for the task; prefer `src/` paths from the user.
2. Follow patterns in existing components and `src/index.css`.
3. Keep changes minimal and aligned with the project stack (Vite + React + TypeScript).

Replace this entire body with instructions specific to your agent (e.g. release checklist, TCG data validation, design review).
