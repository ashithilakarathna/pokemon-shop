# Agent notes

This repo includes Cursor configuration under [`.cursor/`](.cursor/README.md): **rules** (`.mdc`), **subagents** (`.cursor/agents/*.md`), optional **hooks**, **skills**, and **commands**.

When working here, prefer the stack and file layout described in `.cursor/rules/project.mdc`. Layer 1 test-driven development (red → green → refactor) is defined in `.cursor/rules/tdd.mdc`.
